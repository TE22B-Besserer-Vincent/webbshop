/* console.log("Hello!")

function myFunction() {
  console.log("Hello again!")
  console.log(document.getElementsByClassName("dropdown-content"))
}
  
window.onclick = function(event) {
    if (!event.target.matches('.cart-btn') || !event.target.matches('.cart-dropdown')) {
      var dropdown = document.getElementsByClassName("dropdown-content")[0];
      if (dropdown.style.display == "flex") {
        dropdown.style.display = "none"
        document.getElementsByClassName("screen-tint")[0].style.display = "none"
      } else { dropdown.style.display = "flex"; document.getElementsByClassName("screen-tint")[0].style.display = "flex";  }
    }  
}  */